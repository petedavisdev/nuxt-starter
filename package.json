{
	"name": "nuxt-app",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare",
		"check": "eslint . --fix && prettier --write ."
	},
	"dependencies": {
		"nuxt": "^3.15.4",
		"zod": "^3.24.2"
	},
	"devDependencies": {
		"@nuxt/eslint": "^1.1.0",
		"eslint": "^9.20.1",
		"prettier": "^3.5.1",
		"vitest": "^3.0.7"
	}
}
